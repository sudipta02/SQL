select s1.product_id, s1.year as first_year, s1.quantity, s1.price
from sales s1
join (
    select s2.product_id, min(s2.year) first_year
    from sales s2
    group by s2.product_id
) s3
on s3.product_id = s1.product_id and s3.first_year = s1.year